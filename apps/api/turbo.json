{
  "$schema": "https://turborepo.com/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true,
      "passThroughEnv": ["PORT"]
    },
    "build": {
      "outputs": ["dist"],
      "passThroughEnv": ["PORT", "DASHBOARD_URL"]
    }
  }
}