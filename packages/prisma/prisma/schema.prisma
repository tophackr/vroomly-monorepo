generator client {
    provider        = "prisma-client-js"
    output   = "../dist"
    runtime="bun"
}

datasource db {
    provider  = "postgresql"
    url       = env("DATABASE_URL")
    directUrl = env("DIRECT_URL")
}
